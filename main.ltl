// ltl-ode-rk4/main.ltl
//
// Runge-Kutta 4 ODE integrator with adaptive step size.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-ode-rk4 v0.1.0")
    io::println("Runge-Kutta 4 ODE integrator with adaptive step size.")
    0
}
